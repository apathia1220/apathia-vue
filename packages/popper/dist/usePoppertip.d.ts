import Popper from './Popper.vue';
import type { PopperProps as PopperPropType } from './types';
export declare function usePoppertip(): {
    popper: (props: PopperPropType) => void;
};
export default Popper;
