export declare type CollapseProps = {
    expand: boolean;
    duration: number;
    header: string;
    showArrow: boolean;
    disabled: boolean;
};
export declare type TransitionProps = {
    duration: number;
};
